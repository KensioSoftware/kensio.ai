---
version: 2

updates:
  - package-ecosystem: "npm"
    directories:
      - "/"
    schedule:
      interval: "daily"
      time: "08:00"
      timezone: "Europe/London"
    cooldown:
      semver-major-days: 30
      semver-minor-days: 7
      semver-patch-days: 5
    open-pull-requests-limit: 5
    groups:
      npm-patch-and-minor:
        patterns:
          - "*"
        update-types:
          - "patch"
          - "minor"
    # "build" is one of the types pr-title.yml accepts, and it releases nothing,
    # so a merged bump does not cut a version on its own.
    commit-message:
      prefix: "build"
      include: "scope"
    labels:
      - "dependencies"
    reviewers:
      - "hughgrigg"

  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "daily"
      time: "08:00"
      timezone: "Europe/London"
    cooldown:
      default-days: 7
    groups:
      github-actions:
        patterns:
          - "*"
    open-pull-requests-limit: 3
    commit-message:
      prefix: "build"
      include: "scope"
    labels:
      - "dependencies"
    reviewers:
      - "hughgrigg"
