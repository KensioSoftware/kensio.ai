# Copy to .env and fill in. .env is gitignored, this file is not.

# Pangram REST API key, used only by opt-in prose checks that are run by hand.
# Never read by the release workflow, npm run check, or anything in plugins/.
# https://docs.pangram.com/quickstart-rest
PANGRAM_API_KEY=
